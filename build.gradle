buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.4.2"
    }
}

// ✅ DO NOT add allprojects { repositories } here (we use settings.gradle)
tasks.register("clean", Delete) {
    delete(rootProject.buildDir)
}
